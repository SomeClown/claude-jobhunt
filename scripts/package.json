{
  "name": "jobhunt-render",
  "version": "0.1.0",
  "private": true,
  "description": "Markdown-to-docx renderer for jobhunt resumes and cover letters. Reads jobs/<slug>/resume.md and cover-letter.md and produces ATS-friendly .docx files.",
  "type": "commonjs",
  "main": "render.js",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "docx": "^9.0.0"
  }
}
