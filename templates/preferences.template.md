# Job Preferences

*Last updated: YYYY-MM-DD*

## Target Roles
- (List target job titles, one per line — e.g. "Director of IT Infrastructure".
  Note whether adjacent title bands, like a Senior Manager title alongside a
  Director one, are in scope or filtered out on title alone.)

## Location
(Where you're based, and which location models you'll accept: remote anywhere,
remote within the US, hybrid, on-site within commuting distance, or open to
relocation. Be specific — this drives a hard filter, not a soft preference.)

## Compensation
(Your comp floor and target. State clearly whether this is base only or includes
bonus/equity, e.g. "$150,000 - $200,000 base.")

## Must-Haves
- (Anything a role must have to be worth considering. "None specified" is a valid
  answer.)

## Dealbreakers
- (Anything that rules a role out immediately: specific employers, a location
  model you won't do, a comp ceiling below your floor.)

## Nice-to-Haves
- (Not required, but scored as a plus when present.)

## Company-Specific Exceptions
- (Any per-company overrides to the rules above — e.g. "at Company X, individual-
  contributor roles are in scope, evaluated case by case." Delete this section's
  placeholder if you have none.)

## Notes
(Anything about how these preferences were reached that's worth remembering later
— what was confirmed directly by you and when, and what's still provisional.)

<!--
  Everything below this line is optional. Delete a block entirely if you don't
  need it — an absent section means the documented default applies. See
  reference/data-formats.md -> preferences.md for the full field reference.
-->

## Scan Settings
- last_resolved_days: 90
- last_checked_days: 14
- not_found_retry_days: 180
- unresolved_retry: never
- contact_order: connected_on_desc

## Scoring Overrides
- (Free-form adjustments to the default fit-scoring rubric, e.g. "Comp unlisted at
  a company with no public band caps the rating at Medium." Each override modifies
  the base rubric; it does not replace it.)

## Search Sources
- **(Board name)**
  - board_url: https://example.com/
  - search_url: https://example.com/search?q={keywords}&loc={location}&page={page}
  - notes: (optional — e.g. "results page; never extract full page text")

## Voice
- voice_skill: none
